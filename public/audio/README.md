# Audio Assets

Place the founder's message recording here as `founder-message.mp3`.

The `<FounderMessage />` component on the landing page will pick it up automatically.
If the file is missing, the player shows a graceful "Audio recording coming soon" fallback.
